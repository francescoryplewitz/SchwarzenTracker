generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "darwin-arm64"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id             Int           @id @default(autoincrement())
  lastName       String?
  firstName      String?
  email          String?
  mobile         String?
  roles          String[]
  externalId     String?       @unique
  createdAt      DateTime      @default(now())
  modifiedAt     DateTime      @updatedAt
  isActive       Boolean       @default(true)
  filterSettings Json          @default("{}")
  locale         String        @default("en")

  // Exercise relations
  createdExercises  Exercise[]
  exerciseFavorites UserExerciseFavorite[]
  exerciseNotes     ExerciseNote[]

  // Plan relations
  createdPlans   TrainingPlan[]
  planFavorites  UserPlanFavorite[]

  // Workout relations
  workouts       Workout[]

  // Progress relations
  planExerciseProgressSets PlanExerciseProgressSet[]
}

// ============================================
// UEBUNGEN MODULE
// ============================================

model Exercise {
  id            String   @id @default(cuid())
  name          String
  description   String   @db.Text

  // Kategorisierung
  muscleGroups  MuscleGroup[]
  category      ExerciseCategory
  equipment     Equipment?

  // Medien
  images        ExerciseImage[]
  videoUrl      String?

  // Empfohlene Pausenzeit zwischen Sätzen (in Sekunden)
  recommendedRestSeconds  Int?

  // Varianten
  variants      ExerciseVariant[]
  translations  ExerciseTranslation[]

  // Herkunft
  isSystem      Boolean  @default(false)
  forkedFromId  String?
  forkedFrom    Exercise? @relation("ExerciseForks", fields: [forkedFromId], references: [id])
  forks         Exercise[] @relation("ExerciseForks")
  createdById   Int?
  createdBy     User?     @relation(fields: [createdById], references: [id])

  // Timestamps
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  // Relations
  userFavorites UserExerciseFavorite[]
  exerciseNotes ExerciseNote[]
  planExercises PlanExercise[]
}

model ExerciseNote {
  id         String   @id @default(cuid())
  userId     Int
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  exerciseId String
  exercise   Exercise @relation(fields: [exerciseId], references: [id], onDelete: Cascade)
  note       String   @db.Text
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@unique([userId, exerciseId])
}

model ExerciseVariant {
  id          String    @id @default(cuid())
  title       String
  description String?   @db.Text
  equipment   Equipment?

  exerciseId  String
  exercise    Exercise  @relation(fields: [exerciseId], references: [id], onDelete: Cascade)

  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  translations ExerciseVariantTranslation[]
}

model ExerciseTranslation {
  exerciseId  String
  locale      String
  name        String
  description String   @db.Text

  exercise    Exercise @relation(fields: [exerciseId], references: [id], onDelete: Cascade)

  @@id([exerciseId, locale])
}

model ExerciseVariantTranslation {
  exerciseVariantId String
  locale            String
  title             String
  description       String?   @db.Text

  exerciseVariant   ExerciseVariant @relation(fields: [exerciseVariantId], references: [id], onDelete: Cascade)

  @@id([exerciseVariantId, locale])
}
model ExerciseImage {
  id         String   @id @default(cuid())
  exerciseId String
  url        String
  sortOrder  Int      @default(0)
  altText    String?

  exercise   Exercise @relation(fields: [exerciseId], references: [id], onDelete: Cascade)
}

model UserExerciseFavorite {
  userId      Int
  exerciseId  String
  createdAt   DateTime @default(now())

  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  exercise    Exercise @relation(fields: [exerciseId], references: [id], onDelete: Cascade)

  @@id([userId, exerciseId])
}

// Enums
enum MuscleGroup {
  CHEST
  BACK
  SHOULDERS
  BICEPS
  TRICEPS
  FOREARMS
  ABS
  OBLIQUES
  QUADS
  HAMSTRINGS
  GLUTES
  CALVES
}

enum ExerciseCategory {
  COMPOUND
  ISOLATION
  CARDIO
  STRETCHING
}

enum Equipment {
  BARBELL
  DUMBBELL
  MACHINE
  CABLE
  BODYWEIGHT
  KETTLEBELL
  BAND
  OTHER
}

// ============================================
// TRAININGSPLAN MODULE
// ============================================

model TrainingPlan {
  id          String   @id @default(cuid())
  name        String
  description String?  @db.Text

  isSystem    Boolean  @default(false)
  createdById Int?
  createdBy   User?    @relation(fields: [createdById], references: [id])

  exercises   PlanExercise[]
  favorites   UserPlanFavorite[]
  workouts    Workout[]
  translations TrainingPlanTranslation[]

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model TrainingPlanTranslation {
  trainingPlanId String
  locale         String
  name           String
  description    String?  @db.Text

  trainingPlan   TrainingPlan @relation(fields: [trainingPlanId], references: [id], onDelete: Cascade)

  @@id([trainingPlanId, locale])
}

model PlanExercise {
  id           String   @id @default(cuid())
  planId       String
  plan         TrainingPlan @relation(fields: [planId], references: [id], onDelete: Cascade)
  exerciseId   String
  exercise     Exercise @relation(fields: [exerciseId], references: [id])
  dayType      PlanDayType @default(BOTH)

  sortOrder    Int      @default(0)
  sets         Int      @default(3)
  minReps      Int      @default(8)
  maxReps      Int      @default(12)
  targetWeight Float?
  restSeconds  Int?
  notes        String?  @db.Text

  planSets     PlanExerciseSet[]
  progressSets PlanExerciseProgressSet[]
  workoutSets  WorkoutSet[]

  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@unique([planId, exerciseId])
}

model PlanExerciseProgressSet {
  id             String   @id @default(cuid())
  userId         Int
  user           User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  planExerciseId String
  planExercise   PlanExercise @relation(fields: [planExerciseId], references: [id], onDelete: Cascade)

  setNumber      Int
  weight         Float?
  reps           Int

  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@unique([userId, planExerciseId, setNumber])
}

model PlanExerciseSet {
  id             String   @id @default(cuid())
  planExerciseId String
  planExercise   PlanExercise @relation(fields: [planExerciseId], references: [id], onDelete: Cascade)

  setNumber      Int
  targetWeight   Float?
  targetMinReps  Int
  targetMaxReps  Int

  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@unique([planExerciseId, setNumber])
}

model UserPlanFavorite {
  userId    Int
  planId    String
  createdAt DateTime @default(now())

  user      User         @relation(fields: [userId], references: [id], onDelete: Cascade)
  plan      TrainingPlan @relation(fields: [planId], references: [id], onDelete: Cascade)

  @@id([userId, planId])
}

// ============================================
// WORKOUTS MODULE
// ============================================

model Workout {
  id            String        @id @default(cuid())
  planId        String
  plan          TrainingPlan  @relation(fields: [planId], references: [id])
  userId        Int
  user          User          @relation(fields: [userId], references: [id])

  planName      String        // Snapshot des Plannamens
  dayType       WorkoutDayType?
  status        WorkoutStatus @default(IN_PROGRESS)
  startedAt     DateTime      @default(now())
  completedAt   DateTime?
  pausedAt      DateTime?     // Wann pausiert (null wenn läuft)
  totalPausedMs Int           @default(0) // Akkumulierte Pausenzeit

  sets          WorkoutSet[]

  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
}

model WorkoutSet {
  id              String       @id @default(cuid())
  workoutId       String
  workout         Workout      @relation(fields: [workoutId], references: [id], onDelete: Cascade)

  planExerciseId  String
  planExercise    PlanExercise @relation(fields: [planExerciseId], references: [id])

  setNumber       Int          // 1, 2, 3...

  // Vom Nutzer eingetragen
  weight          Float?
  reps            Int?

  // Zielwerte (Snapshot aus Plan)
  targetWeight    Float?
  targetMinReps   Int
  targetMaxReps   Int
  restSeconds     Int?

  completedAt     DateTime?
  sortOrder       Int          @default(0)

  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt
}

enum WorkoutStatus {
  IN_PROGRESS
  PAUSED
  COMPLETED
  ABANDONED
}

enum PlanDayType {
  BOTH
  A
  B
}

enum WorkoutDayType {
  A
  B
}
